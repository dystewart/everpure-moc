# Tests

In this directory are tests for verifying Pure storage configuration & network and data isolation. Files for each test are 

## Prerequisites 

These tests assume connectivity to the Pure api server via OpenShift. Deploy the pod using the deployment in the cluster to configure tooling and authentication with the 